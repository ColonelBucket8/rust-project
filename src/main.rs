mod variables;
mod guessing_game;

use crate::variables::variables;
use crate::variables::enter_array;
use crate::guessing_game::guessing_game;

fn main() {
    // guessing_game();
    variables();
    enter_array();
}
